const API='https://api.cedarmc.org', $=s=>document.querySelector(s);let products=[];const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function api(p,o={}){const r=await fetch(API+p,{credentials:'include',headers:{'Content-Type':'application/json'},...o});let d={};try{d=await r.json()}catch{}if(!r.ok)throw new Error(d.error||'Request failed');return d}function toast(t){$('#toast').textContent=t;$('#toast').classList.add('show');setTimeout(()=>$('#toast').classList.remove('show'),1800)}function render(){ $('#rows').innerHTML=products.map((p,i)=>`<div class="editrow" data-i="${i}"><input data-k="name" value="${esc(p.name)}" placeholder="Name"><select data-k="category"><option ${p.category==='ranks'?'selected':''}>ranks</option><option ${p.category==='shards'?'selected':''}>shards</option><option ${p.category==='cosmetics'?'selected':''}>cosmetics</option><option ${p.category==='keys'?'selected':''}>keys</option><option ${p.category==='pets'?'selected':''}>pets</option></select><input data-k="price" type="number" step="0.01" value="${Number(p.price||0)}"><input data-k="description" value="${esc(p.description)}" placeholder="Description"><label><input data-k="enabled" type="checkbox" ${p.enabled!==false?'checked':''}> Live</label><input data-k="badge" value="${esc(p.badge)}" placeholder="Badge"><input data-k="icon" value="${esc(p.icon)}" placeholder="Icon"><input data-k="sort" type="number" value="${Number(p.sort||0)}"><button class="account-btn del">Delete</button></div>`).join('');document.querySelectorAll('.editrow').forEach(row=>row.querySelectorAll('input,select').forEach(x=>x.oninput=()=>{const i=+row.dataset.i,k=x.dataset.k;products[i][k]=x.type==='checkbox'?x.checked:x.type==='number'?Number(x.value):x.value}));document.querySelectorAll('.del').forEach((b,i)=>b.onclick=()=>{products.splice(i,1);render()})}async function start(){try{await api('/api/auth/me');products=await api('/api/store/admin/products');$('#loginBox').classList.add('hidden');$('#editor').classList.remove('hidden');render()}catch{}}$('#add').onclick=()=>{products.push({id:'product-'+Date.now(),category:'ranks',name:'New Product',price:0,icon:'✦',badge:'NEW',description:'',enabled:false,sort:products.length*10});render()};$('#save').onclick=async()=>{try{products=await api('/api/store/admin/products',{method:'POST',body:JSON.stringify({products})});render();toast('Store saved')}catch(e){toast(e.message)}};start();
